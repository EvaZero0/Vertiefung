// deklariere Variable
int anzahlBestellungen; // Datentyp Bezeichner
anzahlBestellungen = 7; // Zuweisung: links Ziel = Quelle

char gedrueckteTaste = 'A'; // Deklaration und Initialisierung, Ende immer mit Semikolon

string titel = "Wochenbericht";

const double PI = 3.14; // Konstanten -> unveränderlich

Console.WriteLine(titel);

// var eineVariable; // eine nicht definierte Variable MUSS gleich einen Wert bekommen!